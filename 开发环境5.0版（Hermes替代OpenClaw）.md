# 开发环境5.0版（Hermes替代OpenClaw）

> 用途：本文件用于替换《开发环境4.6版.md》，作为当前真实开发环境、协作边界、操作基线与后续开发参考文档。  
> 基线来源：《开发环境4.6版.md》与《从OpenClaw切换到Hermes后的开发环境变化总结（基于开发环境4.6）》。  
> 口径：以当前已经实际落地、验证通过、可复验的环境为准，不再以早期 OpenClaw 规划稿为准。  
> 版本日期：2026-05-02

---

## 1. 一句话结论

当前开发环境已经从：

```text
开发环境 4.6：
本地 Surface + GitHub + Codex 为主，云上 OpenClaw / 灵犀保留。
```

演进为：

```text
开发环境 5.0：
本地 Surface + GitHub + Codex 仍为默认主开发路径；
云上主运行体已经从 OpenClaw 切换为 Hermes；
Hermes 已在飞书侧正式接管；
云上灵犀开始更接近真实项目仓库的远程协作入口。
```

因此，5.0 的核心不是推翻 4.6，而是在 4.6 的稳定本地主路径上，把云上侧从 **OpenClaw 时代** 更新为 **Hermes 时代**。

---

## 2. 5.0 相比 4.6 的核心变化

### 2.1 没变的部分

以下内容继续沿用 4.6 口径：

- 本地 `D:\dev\carrey-test-repo-1` 仍然是默认主开发目录。
- GitHub 仍然是唯一事实版本库。
- 当前主要工作分支仍然是 `work/lingxi-codex-bootstrap`。
- 本地 `dev-entry / verify-dev-entry / repo-health-check` 这条轻量验收链仍然成立。
- 本地 Codex 仍然是最适合处理轻量任务的默认执行入口。
- Cursor 仍然是主编辑器。
- ChatGPT Project 仍然是方案、文档、操作单、验收口径的中枢。

统一口径：

```text
4.6 的“本地主路径”没有失效。
5.0 是在 4.6 基础上，把云上主运行体从 OpenClaw 更新为 Hermes。
```

### 2.2 已变化的部分

#### 变化 A：云上主运行体从 OpenClaw 变成 Hermes

4.6 口径：

```text
云上 OpenClaw / 灵犀保留为重任务与扩展能力。
```

5.0 口径：

```text
云上现网主入口已经是 Hermes。
OpenClaw 已不再承担当前对外主入口。
```

后续不要再把云上执行层描述为“OpenClaw 常驻”。

#### 变化 B：云主机类型已明确为 Tencent Cloud CVM

本轮切换中已确认当前实例类型为：

```text
Tencent Cloud CVM
```

后续涉及云服务器控制台操作时，应按 CVM 口径理解，而不是继续按“轻量应用服务器 Lighthouse”默认口径理解。

#### 变化 C：Hermes 已在飞书侧正式接管

当前平台侧结论：

```text
飞书新应用：灵犀-Hermes-Tencent
当前主入口：飞书 / Lark
当前云上主服务：hermes-gateway.service
服务状态：持续 active
hermes chat 运行级探针：ok
```

因此，当前可以明确记录：

```text
云上 Hermes 已完成现网接管。
OpenClaw 已不再是当前现网主入口。
```

#### 变化 D：云上运行用户与运行方式变了

切换过程中的中间状态曾出现过 root 用户下运行 Hermes。当前真实现网状态应按下面口径理解：

```text
当前运行形态：Hermes 应用模板环境
当前运行用户：ubuntu
当前主服务：ubuntu 用户级 hermes-gateway.service
```

后续排查云上服务时，优先按 `ubuntu` 用户环境检查，不要默认回到早期 root 口径。

#### 变化 E：云上项目工作区开始靠近 4.6

为了让飞书 / Telegram 上的灵犀更像“远程开发协作入口”，云上已建立真实项目工作区：

```text
/home/ubuntu/workspace/repos/carrey-test-repo-1
```

当前分支：

```text
work/lingxi-codex-bootstrap
```

当前 Hermes 默认工作目录已切到该项目仓库，并已补充项目级 `HERMES.md`。

飞书侧已实测可返回：

```text
pwd
git rev-parse --show-toplevel
git branch --show-current
git status --short --branch
```

最近一次返回显示：

```text
/home/ubuntu/workspace/repos/carrey-test-repo-1
/home/ubuntu/workspace/repos/carrey-test-repo-1
work/lingxi-codex-bootstrap
## work/lingxi-codex-bootstrap...origin/work/lingxi-codex-bootstrap
?? HERMES.md
```

这说明：

```text
当前飞书上的灵犀已经不再只围绕 Hermes 自己的目录工作，而是已经开始站在真实项目仓库里工作。
```

---

## 3. 当前环境总体架构

现在建议把整个开发环境理解为三层结构。

### 3.1 第一层：本地主开发环境

这是最稳定、最标准、最接近原 4.6 定义的部分。

```text
目录：D:\dev\carrey-test-repo-1
分支：work/lingxi-codex-bootstrap
工具：Windows 11 / Surface Laptop 4 / PowerShell / Git / Cursor / Codex / ChatGPT Project
```

适合任务：

- 文档新增、修订、收口。
- 小范围脚本文案调整。
- PowerShell 脚本验收。
- 本地 diff 检查。
- commit / push。
- Codex 低风险轻量任务。
- 最终人工验收。

当前定位：

```text
默认主开发入口。
```

### 3.2 第二层：云上现网 Hermes 环境

这是当前已经接管飞书主入口的 Hermes 运行态。

```text
平台：Tencent Cloud CVM
运行形态：Hermes 应用模板环境
运行用户：ubuntu
主服务：hermes-gateway.service
主入口：飞书 / Lark
飞书应用：灵犀-Hermes-Tencent
```

适合任务：

- 消息入口。
- 远程轻量协作。
- 云上巡检。
- 长链自动化任务。
- 云上只读检查。
- 需要常驻服务的任务。

当前定位：

```text
云上现网主运行体。
```

### 3.3 第三层：云上“靠近 4.6”的项目工作区

```text
目录：/home/ubuntu/workspace/repos/carrey-test-repo-1
分支：work/lingxi-codex-bootstrap
```

作用：

- 让飞书 / Telegram 上的灵犀围绕真实项目仓库工作。
- 支持远程查看 repo 状态。
- 支持轻量只读分析。
- 支持云上巡检报告。
- 让云上 Hermes 从“消息机器人”升级为“远程协作入口”。

当前定位：

```text
本地 4.6 / 5.0 的远程辅助工作区。
```

注意：这层不是本地开发环境的替代品。

---

## 4. 当前主要资源与实际配置

### 4.1 本地设备

```text
Surface Laptop 4
Windows 11
PowerShell
Cursor
Codex
Git
```

当前本地职责：

- 打开仓库目录。
- 编辑与查看 Markdown / PowerShell / 配置文件。
- 运行本地验收脚本。
- 使用 Codex 做轻量任务。
- 最终确认 diff。
- 进行 Git 提交与推送。

### 4.2 本地仓库目录

```text
D:\dev\carrey-test-repo-1
```

这是当前默认的本地开发根目录。

### 4.3 本地中转目录

```text
D:\dev\_xfer_lingxi\upload
D:\dev\_xfer_lingxi\download
```

当前角色：

- `upload`：本地准备传到服务器的临时文件。
- `download`：从服务器取回报告、备份、导出物。

当前原则：

```text
xfer 目录只做中转，不当长期主存储。
```

### 4.4 本地 OpenClaw 切换备份

本次切换材料应继续离线保留：

```text
D:\dev\_xfer_lingxi\download\openclaw-cutover-20260430\openclaw-cutover
```

它现在不是运行必需品，但有三类价值：

- 回滚参考。
- 审计参考。
- 迁移材料留档。

不要随手删除。

### 4.5 GitHub 仓库

```text
GitHub 用户名：carrey578
仓库：carrey-test-repo-1
```

仓库定位：

- 测试仓。
- 可接受 AI 高权限试验。
- 适合做环境打通、工作流验证、文档固化、低风险脚本调整。

### 4.6 当前主工作分支

```text
work/lingxi-codex-bootstrap
```

当前推荐口径：

- 轻量协作默认继续在该分支完成。
- 暂不强制切回其他分支。
- 后续若进入真正开发任务，可再按任务单独开分支。
- 云上项目工作区也应保持与该分支对齐。

### 4.7 协作文档目录

```text
docs/collab/chatgpt-lingxi/
```

当前用途：

- 阶段性方案。
- session log。
- playbook。
- closeout 文档。
- 状态记录。
- ChatGPT / Codex / Hermes / 灵犀协作文件。
- OpenClaw → Hermes 切换记录。

该目录仍是当前最重要的协作文档中枢。

---

## 5. 本地工具现状

### 5.1 Git

已正常可用，用于：

- 查看状态。
- 拉取远端。
- 比较 diff。
- 提交与推送。

常用命令：

```powershell
Set-Location "D:\dev\carrey-test-repo-1"
git fetch origin
git pull --ff-only origin work/lingxi-codex-bootstrap
git status
git diff --name-only
git log --oneline -5
git push origin work/lingxi-codex-bootstrap
```

当前 Git 链路口径：

```text
本地 pull / fetch / push 已恢复稳定可用。
```

### 5.2 Cursor

当前角色：

- 主编辑器。
- diff 查看器。
- Markdown 文件人工检查入口。
- PowerShell 文件人工检查入口。

### 5.3 Node / npm

当前已知可用版本沿用 4.6 口径：

```text
Node.js：v24.14.0
npm：11.9.0
```

说明：

- 当前脚本链路可运行。
- 不需要因为进入 5.0 而额外升级 Node / npm。
- 后续如无明确报错，不建议主动折腾版本。

### 5.4 Codex CLI / Codex Desktop

当前状态：

```text
已登录
已可执行
已完成实际轻量任务验证
```

当前建议定位：

```text
本地低风险轻量任务的默认执行工具。
```

适合：

- 文档补充。
- session log 更新。
- 小范围脚本文案修改。
- 只读分析。
- diff 摘要。
- 生成操作单。
- 生成验收清单。

不适合默认放开的任务：

- 大范围多文件重构。
- 无边界的自动修改。
- 没有验收脚本的直接提交。
- 系统级云端配置修改。

---

## 6. 云上 Hermes 环境现状

### 6.1 当前状态结论

```text
OpenClaw 已退出当前现网主入口。
Hermes 已成为当前云上主运行体。
飞书侧 Hermes 已正式接管。
```

### 6.2 当前云上运行态

```text
平台：Tencent Cloud CVM
运行形态：Hermes 应用模板环境
运行用户：ubuntu
主服务：hermes-gateway.service
主入口：飞书 / Lark
飞书应用：灵犀-Hermes-Tencent
项目工作区：/home/ubuntu/workspace/repos/carrey-test-repo-1
项目分支：work/lingxi-codex-bootstrap
```

### 6.3 当前云上服务检查命令

通过 SSH 登录云机后，建议优先按 `ubuntu` 用户检查。

检查 Hermes gateway：

```bash
sudo -iu ubuntu bash -lc 'systemctl --user status hermes-gateway.service --no-pager'
```

预期：

```text
active
```

检查项目工作区：

```bash
sudo -iu ubuntu bash -lc 'cd /home/ubuntu/workspace/repos/carrey-test-repo-1 && pwd && git branch --show-current && git status --short --branch'
```

预期：

```text
/home/ubuntu/workspace/repos/carrey-test-repo-1
work/lingxi-codex-bootstrap
## work/lingxi-codex-bootstrap...origin/work/lingxi-codex-bootstrap
```

如果仍看到：

```text
?? HERMES.md
```

说明云上项目级 `HERMES.md` 仍是未跟踪文件，属于当前已知状态，不要误判为异常。

检查 OpenClaw 是否残留占用主入口：

```bash
ps aux | grep -Ei "openclaw|claw|hermes|node|python" | grep -v grep || true
ss -lntp | grep -Ei "openclaw|claw|hermes|node|python" || true
```

判断口径：

- Hermes 相关进程存在：正常。
- OpenClaw 主 gateway 仍占用消息通道：需要进一步排查。
- 单纯残留日志 / 备份 / 旧目录：不等于主入口仍被 OpenClaw 占用。

### 6.4 当前云上工作区与本地的关系

本地：

```text
D:\dev\carrey-test-repo-1
```

云上：

```text
/home/ubuntu/workspace/repos/carrey-test-repo-1
```

关系：

- 两者应通过 GitHub 同步。
- 不应直接把两边当成同一个物理目录。
- 本地仍是默认提交点。
- 云上更适合只读巡检、轻量协作与长链任务。
- 若云上要写回，应先明确任务边界，并在 `docs/collab/chatgpt-lingxi/` 留痕。

---

## 7. 当前脚本链路的实际可用状态

### 7.1 dev-entry 统一入口

命令：

```powershell
.\tools\dev-entry.ps1 help
```

当前定位：

```text
本地最简统一开发入口。
```

### 7.2 verify-dev-entry 验证脚本

命令：

```powershell
.\tools\verify-dev-entry.ps1
```

当前定位：

```text
面向 dev-entry.ps1 的快速功能回归脚本。
```

### 7.3 repo-health-check 健康检查

命令：

```powershell
.\tools\repo-health-check.ps1
```

当前定位：

```text
本地最简总体验收入口。
```

### 7.4 preflight-check

命令：

```powershell
.\tools\preflight-check.ps1
```

当前定位：

```text
提交前低成本自检脚本。
```

---

## 8. 当前推荐工作流

### 8.1 默认主路径：本地轻量开发

适用场景：

- 文档修改。
- 小范围脚本文案调整。
- session log 更新。
- 操作指南补充。
- 低风险修订。
- 需要提交到 GitHub 的变更。

操作步骤：

```powershell
Set-Location "D:\dev\carrey-test-repo-1"
git fetch origin
git pull --ff-only origin work/lingxi-codex-bootstrap
git status
```

用 Codex / Cursor 完成修改后：

```powershell
.\tools\dev-entry.ps1 help
.\tools\verify-dev-entry.ps1
.\tools\repo-health-check.ps1
git diff --name-only
git diff
```

确认无误后：

```powershell
git add <files>
git commit -m "<message>"
git push origin work/lingxi-codex-bootstrap
```

预期效果：

- 本地可复验。
- diff 可解释。
- GitHub 有正式记录。
- 不依赖云上即时状态。

### 8.2 补充路径：飞书 / Telegram 远程协作

适用场景：

- 不在本机旁边。
- 需要让灵犀看当前 repo 状态。
- 需要云上执行只读巡检。
- 需要远程生成简短报告。
- 需要检查 Hermes gateway / 云上服务状态。

适合让 Hermes / 灵犀做：

```text
1. 查看当前仓库分支
2. 查看 git status
3. 查看某个文件并总结
4. 查看最近 commit
5. 运行轻量只读命令
6. 做云上巡检报告
7. 生成远程任务摘要
```

不建议默认让 Hermes / 灵犀做：

```text
1. 大范围多文件修改
2. 复杂脚本重构
3. 依赖升级
4. 系统级配置大改
5. 无验收的直接提交
6. 与本地 Codex 同时修改同一批文件
```

当前口径：

```text
飞书 / Telegram 上的灵犀是远程协作入口、只读/轻量开发助手，不是本地 5.0 的完全替代品。
```

### 8.3 云上长任务路径

适用场景：

- 需要长时间运行。
- 需要常驻服务。
- 需要云上巡检。
- 需要通过消息入口触发。
- 本地电脑不适合长时间运行。

建议流程：

```text
1. 先由 ChatGPT / 人工定义任务边界
2. 如果涉及仓库，先确认云上分支和 git status
3. 优先只读运行
4. 如需写入，先写到 docs/collab/chatgpt-lingxi/
5. 通过 GitHub 形成可追溯记录
6. 本地最终验收
```

---

## 9. 当前协作边界

### 9.1 现在该做

```text
1. 保持本地 5.0 主路径稳定
2. 保留 OpenClaw 切换备份
3. 使用 Hermes 作为云上主运行体
4. 让飞书 / Telegram 灵犀优先做只读、轻量、巡检类任务
5. 用 Codex 处理本地文档和低风险脚本任务
6. 所有重要变更通过 GitHub 留痕
7. 对云上 HERMES.md 是否纳入版本库做一次后续决策
```

### 9.2 暂时不要做

```text
1. 不要把 OpenClaw 恢复成现网主入口
2. 不要把云上 Hermes 当成本地 Windows 4.6 的完全镜像
3. 不要同时让本地 Codex 和云上 Hermes 改同一批文件
4. 不要在云上直接做大范围业务代码修改
5. 不要绕过 GitHub 作为事实版本库
6. 不要删除 openclaw-cutover 本地备份
7. 不要把 root 用户口径当成当前 Hermes 现网默认口径
8. 不要为了“升级环境”继续制造无必要的新阶段
```

---

## 10. 当前常见任务分配

### 10.1 交给 ChatGPT

适合：

- 方案设计。
- 操作手册。
- 验收清单。
- 故障排查清单。
- 决策建议。
- 文档结构设计。
- Codex / Hermes 指令包生成。

### 10.2 交给 Codex

适合：

- 本地 repo 轻量修改。
- Markdown 文档补齐。
- 小范围脚本文案修改。
- 运行本地验收脚本。
- 输出 diff 摘要。
- 写 session log / closeout。

执行前置条件：

```text
本地 repo 已同步
任务边界清楚
只改允许的目录或文件
有验收脚本
不写密钥
不自动 push，除非人工明确授权
```

### 10.3 交给 Hermes / 灵犀

适合：

- 云上巡检。
- 飞书 / Telegram 远程问答。
- 查看云上 repo 状态。
- 检查 Hermes gateway 状态。
- 长链自动化任务。
- 生成云上报告。
- 必要时写协作文档。

执行前置条件：

```text
确认当前目录是 /home/ubuntu/workspace/repos/carrey-test-repo-1
确认分支是 work/lingxi-codex-bootstrap
确认 git status
优先只读
如需写入，限制在 docs/collab/chatgpt-lingxi/
不直接做大范围重构
```

### 10.4 必须人工执行

必须人工确认或执行：

- 腾讯云控制台关键操作。
- API key / OAuth / 模型授权。
- 是否切换主入口。
- 是否恢复 / 回滚。
- 是否把 `HERMES.md` 纳入版本库。
- 是否允许 Codex / Hermes push。
- 最终验收与边界判断。

---

## 11. 当前验收清单

### 11.1 本地 5.0 验收

```powershell
Set-Location "D:\dev\carrey-test-repo-1"
git fetch origin
git status
.\tools\dev-entry.ps1 help
.\tools\verify-dev-entry.ps1
.\tools\repo-health-check.ps1
git diff --name-only
```

通过标准：

```text
1. 仓库可进入
2. 当前分支可解释
3. 本地验收脚本能运行
4. diff 可解释
5. 无未授权大范围变更
```

### 11.2 云上 Hermes 验收

```bash
sudo -iu ubuntu bash -lc 'systemctl --user status hermes-gateway.service --no-pager'
sudo -iu ubuntu bash -lc 'cd /home/ubuntu/workspace/repos/carrey-test-repo-1 && pwd && git branch --show-current && git status --short --branch'
```

通过标准：

```text
1. hermes-gateway.service 为 active
2. 当前目录为 /home/ubuntu/workspace/repos/carrey-test-repo-1
3. 当前分支为 work/lingxi-codex-bootstrap
4. git status 可解释
5. OpenClaw 没有重新占用主消息入口
```

### 11.3 平台侧 Hermes 验收

通过飞书 / Lark 给 `灵犀-Hermes-Tencent` 发送测试消息：

```text
请回复当前 agent 名称、当前工作目录、当前 git 分支。
```

通过标准：

```text
1. 能正常回复
2. 当前工作目录指向项目仓库
3. 当前分支为 work/lingxi-codex-bootstrap
4. 回复内容不是旧 OpenClaw 入口产生
```

---

## 12. 当前风险与修补口径

### 12.1 风险：云上 HERMES.md 未跟踪

现象：

```text
?? HERMES.md
```

当前判断：

```text
已知状态，不是紧急异常。
```

后续处理选项：

```text
A. 纳入版本库：如果它对项目上下文确实有长期价值
B. 保持云上本地文件：如果只是 Hermes 本地提示文件
C. 改名或迁入 docs/collab/chatgpt-lingxi/：如果要作为协作文档留存
```

当前建议：

```text
暂不急着处理。下一轮单独做一次 HERMES.md 留存决策。
```

### 12.2 风险：把 Hermes 当成本地完整开发环境

现象：

```text
直接让飞书 Hermes 做大范围修改、执行 Windows PowerShell 流程、直接提交。
```

修补口径：

```text
回到本地 Surface + Codex + 验收脚本主路径。
```

### 12.3 风险：OpenClaw 旧口径继续污染文档

现象：

```text
文档继续写“云上 OpenClaw / 灵犀为主运行体”。
```

修补口径：

```text
统一替换为“云上 Hermes / 灵犀为现网主运行体；OpenClaw 已退出现网主入口，仅作为历史迁移来源与备份参考”。
```

### 12.4 风险：云上与本地分支状态不一致

本地检查：

```powershell
Set-Location "D:\dev\carrey-test-repo-1"
git fetch origin
git status
```

云上检查：

```bash
sudo -iu ubuntu bash -lc 'cd /home/ubuntu/workspace/repos/carrey-test-repo-1 && git fetch origin && git status --short --branch'
```

如果云上有未提交改动，先不要 pull / reset，先记录并判断是否需要保留。

---

## 13. 当前是否需要继续升级环境

当前结论：

```text
不需要立刻继续升级环境。
```

原因：

1. 本地 4.6 主路径仍然稳定，已延续为 5.0 主路径。
2. 云上 Hermes 已接替 OpenClaw 成为现网主运行体。
3. 飞书侧已完成 Hermes 接管。
4. 云上项目工作区已能围绕真实仓库工作。
5. 当前更应该进入稳定使用态，而不是继续做环境工程。

下一步更合理的方向：

```text
先用 5.0 做真实小任务；
再根据真实需求决定是否接入 OpenAI WS Agents、Agent Harness 或更多云上自动化。
```

---

## 14. 后续建议

### 14.1 把 5.0 作为当前唯一环境说明基线

后续若再看环境说明，优先看：

```text
开发环境5.0版（Hermes替代OpenClaw）.md
```

4.6 作为历史基线保留，不再代表当前云上真实状态。

### 14.2 保留本地优先原则

默认流程仍然是：

```text
本地 Surface → Codex / Cursor → 本地验收 → GitHub
```

### 14.3 把 Hermes 定位为云上现网入口 + 远程协作入口

Hermes 适合：

```text
飞书入口
云上巡检
只读远程协作
长链自动化
常驻服务类任务
```

不适合默认替代本地：

```text
Windows PowerShell 验收
大范围代码改动
最终提交决策
```

### 14.4 单独处理 HERMES.md 留存

建议后续开一个轻量任务：

```text
任务：判断云上 HERMES.md 是否纳入版本库
```

验收结果可以是：

```text
A. git add HERMES.md 并补充说明
B. 移入 docs/collab/chatgpt-lingxi/
C. 保留为云上本地配置，不纳入 Git
```

### 14.5 OpenAI WS Agents 暂缓到 Hermes 稳定后

当前不急着把 OpenAI WS Agents 混入 5.0。

建议等：

```text
1. Hermes 现网入口稳定一段时间
2. HERMES.md 处理完成
3. 本地与云上分支状态稳定
4. 有明确 Workspace Agents 使用场景
```

再做下一轮接入。

---

## 15. 当前统一状态口径

后续对外或对后续使用者说明时，可以直接使用下面这段：

> 当前开发环境已经升级为《开发环境5.0版（Hermes替代OpenClaw）》。  
> 它保留了 4.6 的本地主路径：本地 Surface + GitHub + Codex 仍是默认开发与验收入口。  
> 与 4.6 最大不同是：云上主运行体已经从 OpenClaw 切换为 Hermes，并且 Hermes 已在飞书侧正式接管。  
> 当前云上 Hermes 运行在腾讯云 CVM 的 Hermes 应用模板环境中，由 `ubuntu` 用户下的 `hermes-gateway.service` 提供主入口；飞书应用为 `灵犀-Hermes-Tencent`。  
> 同时，云上 Hermes 已配置到真实项目仓库 `/home/ubuntu/workspace/repos/carrey-test-repo-1`，分支为 `work/lingxi-codex-bootstrap`，因此可以作为远程只读协作、轻量巡检和长链任务入口。  
> 但它仍不是本地 Windows 4.6/5.0 环境的完全镜像，后续仍建议：本地负责轻量开发与验收，云上 Hermes 负责远程入口、轻量只读协作与长链自动化任务。

---

## 16. 最常用命令清单

### 16.1 本地进入仓库并同步

```powershell
Set-Location "D:\dev\carrey-test-repo-1"
git fetch origin
git pull --ff-only origin work/lingxi-codex-bootstrap
git status
```

### 16.2 本地验收

```powershell
.\tools\dev-entry.ps1 help
.\tools\verify-dev-entry.ps1
.\tools\repo-health-check.ps1
```

### 16.3 本地查看改动

```powershell
git diff --name-only
git diff
```

### 16.4 本地提交与推送

```powershell
git add <files>
git commit -m "<message>"
git push origin work/lingxi-codex-bootstrap
```

### 16.5 云上检查 Hermes 服务

```bash
sudo -iu ubuntu bash -lc 'systemctl --user status hermes-gateway.service --no-pager'
```

### 16.6 云上检查项目仓库

```bash
sudo -iu ubuntu bash -lc 'cd /home/ubuntu/workspace/repos/carrey-test-repo-1 && pwd && git branch --show-current && git status --short --branch'
```

### 16.7 飞书侧快速验收

向 `灵犀-Hermes-Tencent` 发送：

```text
请回复当前 agent 名称、当前工作目录、当前 git 分支。
```

预期：

```text
能正常回复；
工作目录为 /home/ubuntu/workspace/repos/carrey-test-repo-1；
分支为 work/lingxi-codex-bootstrap。
```

---

## 17. 交给 Codex 的落地指令

如果需要把本文正式写入 repo，可以把下面指令交给 Codex。

```text
任务：将当前环境文档升级为《开发环境5.0版（Hermes替代OpenClaw）》。

工作目录：
D:\dev\carrey-test-repo-1

前置检查：
1. git fetch origin
2. git status
3. 确认当前分支为 work/lingxi-codex-bootstrap

允许操作：
1. 新增或更新文档：开发环境5.0版（Hermes替代OpenClaw）.md
2. 如 repo 已有环境文档目录，可放入合适目录；否则先放仓库根目录或 docs/collab/chatgpt-lingxi/，不要移动其它文件。
3. 不删除《开发环境4.6版.md》，仅将其作为历史基线保留。

禁止操作：
1. 不改业务代码。
2. 不改 tools/。
3. 不改 scripts/。
4. 不写 token / key / 密码。
5. 不自动 push，除非人工明确授权。

完成后输出：
1. git diff --name-only
2. git status
3. 本次文档相对 4.6 的主要变化摘要
```

---

## 18. 最终结论

《开发环境5.0版（Hermes替代OpenClaw）》对应的是一套：

```text
本地 4.6 主路径继续稳定；
云上 OpenClaw 已退出现网主入口；
Hermes 已成为云上现网主运行体；
飞书侧 Hermes 已正式接管；
云上项目工作区已靠近真实项目协作状态；
整体进入 5.0 稳定使用态。
```

最终建议：

```text
日常轻量开发：本地 Surface + Codex + 本地验收 + GitHub
远程协作与长任务：云上 Hermes / 灵犀
最终判断与关键动作：人工确认
```

这就是当前真实、可复验、适合后续继续使用的开发环境主口径。
