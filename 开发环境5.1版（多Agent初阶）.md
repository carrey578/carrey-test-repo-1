# 开发环境5.1版（多Agent初阶）

> 用途：本文件用于补充并升级《开发环境5.0版（Hermes替代OpenClaw）.md》，作为当前真实开发环境、协作边界、操作基线与后续开发参考文档。  
> 版本定位：多 Agent 初阶基线。  
> 版本日期：2026-05-07。  
> 口径：以已经实际落地、验证通过、可复验的环境为准；5.0 作为 Hermes 替代 OpenClaw 的历史基线继续保留。

---

## 1. 一句话结论

开发环境 5.1 的核心结论是：

```text
Hermes + Codex 的初阶多 Agent 协作闭环已经跑通。
Hermes 作为云端主控 Agent。
Codex 作为本地代码施工 Agent。
GitHub 作为唯一代码事实源与 PR 自动化中枢。
Claude 当前作为网页轻量只读 review 角色。
COS 已作为归档层接入。
```

5.1 不是推翻 5.0，而是在 5.0 已完成“OpenClaw -> Hermes”切换的基础上，进一步形成：

```text
云端 Hermes 主控 + 本地 Codex 施工 + GitHub 双通道 PR 自动化 + Claude 轻量 review + COS 归档
```

的初阶多 Agent 开发基线。

---

## 2. 5.1 相比 5.0 的核心变化

### 2.1 沿用 5.0 的部分

- 本地 `D:\dev\carrey-test-repo-1` 仍是默认主开发目录。
- GitHub 仍是唯一事实版本库。
- 当前主要工作分支仍是 `work/lingxi-codex-bootstrap`。
- Codex 仍是本地低风险代码和文档施工入口。
- Cursor 仍作为本地审查和必要手工微调工具。
- ChatGPT Project 仍作为方案、文档、任务拆解和验收口径中枢。
- Hermes 继续作为 OpenClaw 后的云端主运行体。
- OpenClaw 只作为历史迁移来源和后续预留入口参考，不再承担近期主链路。

### 2.2 新增和升级的部分

| 变化项 | 5.0 口径 | 5.1 口径 |
|--------|----------|----------|
| 多 Agent 协作 | Hermes 替代 OpenClaw，云端入口稳定 | Hermes + Codex + GitHub + Claude review 初阶闭环已验证 |
| Hermes | 云端现网主运行体 | 云端主控 Agent，负责任务卡、报告、COS、阶段汇总 |
| Codex | 本地轻量任务执行工具 | 本地代码施工 Agent，已完成真实脚本和 FastAPI demo |
| GitHub | 唯一事实版本库，SSH push 可用 | SSH push、`gh` Draft PR、GitHub 插件 Draft PR 均已验证 |
| Claude | 备用设想 | Claude 网页版轻量只读 review 已完成；Claude Code 尚未正式接入 |
| COS | 规划中的归档层 | 腾讯云 COS smoke test 已通过，作为报告/日志/快照归档层 |

---

## 3. 当前环境总体架构

### 3.1 本地主开发与施工层

```text
位置：本地 Windows / Surface 环境
主目录：D:\dev\carrey-test-repo-1
主分支：work/lingxi-codex-bootstrap
工具：PowerShell / Git / gh / Cursor / Codex Desktop / ChatGPT
```

当前定位：

```text
默认开发入口 + 默认代码施工入口 + 最终人工验收入口。
```

主要职责：

- 本地代码和文档修改。
- 脚本开发与测试执行。
- diff 检查、commit、push。
- 通过 `gh` 创建 Draft PR。
- 结合 Cursor 做人工审查和必要微调。

### 3.2 云端 Hermes 主控层

```text
平台：Tencent Cloud CVM
运行用户：ubuntu
主服务：hermes-gateway.service
消息入口：飞书 / Lark
飞书应用：灵犀-Hermes-Tencent
项目工作区：/home/ubuntu/workspace/repos/carrey-test-repo-1
主分支：work/lingxi-codex-bootstrap
```

当前定位：

```text
云端主控 Agent + 远程协作入口 + 长链任务执行位。
```

主要职责：

- 检查云端 repo 状态。
- 创建任务卡。
- 生成阶段报告、验收报告和汇总报告。
- 执行云端 smoke test。
- 对接 COS 归档层。
- 汇总 Codex 执行结果。
- 维护 `docs/collab/chatgpt-lingxi/` 下的协作文档。

说明：

```text
5.1 当前真实基线以 /home/ubuntu/workspace/repos/carrey-test-repo-1 为准。
不要再默认使用早期 /root/... 或 OpenClaw 主链路口径。
```

### 3.3 GitHub 协作与自动化层

```text
仓库：carrey578/carrey-test-repo-1
主工作分支：work/lingxi-codex-bootstrap
main 策略：禁止直接推送
PR 策略：默认 Draft PR，base 指向 work/lingxi-codex-bootstrap
```

当前已验证两条 PR 自动化通道：

1. 本地 `gh` CLI 通道
   - 登录身份：`carrey578`
   - Git 协议：SSH
   - 已验证能力：读取 repo、读取 PR、创建 Draft PR
   - 证据：PR #3：`https://github.com/carrey578/carrey-test-repo-1/pull/3`

2. Codex GitHub 插件/Connector 通道
   - 执行身份：`carrey578`
   - 仓库授权范围：`carrey578/carrey-test-repo-1`
   - 已验证能力：读取 PR changed files、写 PR 评论、创建 Draft PR
   - 证据：PR #4：`https://github.com/carrey578/carrey-test-repo-1/pull/4`

历史注意事项：

```text
GitHub Connector 曾因实际执行身份为 AnranMeng 而无法创建 PR。
重新授权 Connector 为 carrey578 后，插件创建 PR 能力已恢复。
如再次遇到 must be a collaborator，应优先检查 Connector 实际执行身份。
```

### 3.4 Claude 轻量 review 层

当前 Claude 定位：

```text
备用 review Agent / 第二意见角色。
```

已完成：

- Claude 网页版对 FastAPI health demo 做过轻量只读 review。
- Review 结论：无必须修改项，建议合并，可作为阶段七形式闭环证据。

尚未完成：

- Claude Code 终端版正式接入。
- Claude API / SDK 自动化接入。

建议：

```text
短期不急于接入 Claude API。
如需正式增强 Claude 能力，优先使用 Claude Pro 月付做本地只读 review 验证。
```

### 3.5 COS 归档层

当前状态：

```text
腾讯云 COS 归档层已完成 smoke test。
```

用途：

- 报告归档。
- 日志归档。
- 环境快照归档。
- 构建产物或任务输出归档。

原则：

```text
COS 是归档层，不是本地 repo 主目录。
不把密钥、token、密码写入 repo。
```

---

## 4. 已完成的近期阶段

《多Agent配置-近期阶段指南2.0（基于开发环境5.0）--分阶段实施版》中的阶段 1-8 已完成。

| 阶段 | 内容 | 状态 | 关键证据 |
|------|------|------|----------|
| 阶段1 | Hermes 云端 repo 基线检查 | 已完成 | `docs/collab/chatgpt-lingxi/ops/2026-05-03-env5-recent-stage1-cloud-baseline.md` |
| 阶段2 | 本地 Codex 基线检查 | 已完成 | `docs/collab/chatgpt-lingxi/ops/2026-05-03-env5-recent-stage2-local-codex-baseline.md` |
| 阶段3 | 近期任务模板建立 | 已完成 | `docs/collab/chatgpt-lingxi/tasks/task-template-env5-recent.md` |
| 阶段4 | repo doctor 真实轻量任务 | 已完成 | `scripts/env5_repo_doctor.py` 与 repo doctor reports |
| 阶段5 | COS 归档层接入 | 已完成 | `docs/collab/chatgpt-lingxi/reports/2026-05-05-hermes-cos-smoke-test.md` |
| 阶段6 | FastAPI health demo | 已完成 | `app/main.py`、`tests/test_health.py`、FastAPI change-summary |
| 阶段7 | Claude 网页版轻量只读 review | 已完成 | `docs/collab/chatgpt-lingxi/reports/2026-05-07-claude-web-review-fastapi-health-demo.md` |
| 阶段8 | Hermes 近期阶段总报告 | 已完成并已事实修正 | `docs/collab/chatgpt-lingxi/reports/2026-05-07-env5-recent-stage-summary.md` |

补充收口报告：

```text
docs/collab/chatgpt-lingxi/reports/2026-05-07-env5-initial-stage-closure-and-baseline-addendum.md
```

---

## 5. 当前已验证能力

### 5.1 Codex 已验证能力

- 自动编辑文档和低风险代码。
- 生成 `scripts/env5_repo_doctor.py`。
- 创建最小 FastAPI 应用。
- 增加 `/health` endpoint。
- 增加并运行测试。
- 生成 change-summary。
- 本地 commit / push。
- 使用 `gh pr create` 创建 Draft PR。

当前 FastAPI 测试结果：

```text
3 passed
```

### 5.2 Hermes 已验证能力

- 云端 repo 基线检查。
- 创建共享协作目录。
- 创建任务模板和任务卡。
- 生成执行报告和阶段总报告。
- 执行 COS smoke test。
- 在冲突发生时停止强推，等待人工决策。

### 5.3 GitHub 已验证能力

- SSH Git fetch / push 可用。
- 本地 `gh auth` 已修复并登录为 `carrey578`。
- 本地 `gh` 可创建 Draft PR。
- GitHub 插件可读取 PR、写 PR 评论、创建 Draft PR。
- PR #3、PR #4 均为 Draft，不合并、不关闭，作为验证证据保留。

---

## 6. 当前操作边界

### 6.1 允许的常规操作

- 在 `work/lingxi-codex-bootstrap` 上做低风险文档和代码任务。
- 创建任务卡、report、change-summary。
- 本地运行测试。
- 使用 Draft PR 做流程验证。
- Hermes 执行云端报告和 COS 归档。

### 6.2 禁止或需人工确认的操作

- 不直接 push `main`。
- 不强推覆盖远端。
- 不写入 token、key、密码。
- 不删除 5.0 历史基线文档。
- 不合并或关闭 PR，除非人工明确授权。
- 不让 Claude API / SDK 无人值守接入生产级权限。
- 多 Agent 同时修改同一高冲突文件前应先明确所有权。

高冲突文件类型包括：

- `.gitignore`
- 任务卡
- 阶段 summary
- change-summary
- 测试文件

---

## 7. 当前仍保留的限制

| 限制项 | 当前影响 | 建议 |
|--------|----------|------|
| Claude Code 终端版尚未正式接入 | 不影响 5.1 初阶基线；影响复杂任务备用能力 | 中期按需做只读 review 验证 |
| PR #3 / PR #4 仍为 Draft | 不影响验证；对应 smoke-test 变更未作为主分支业务内容合并 | 保留为证据；如需合并，另行人工授权 |
| 多 Agent 并行仍可能产生 rebase 冲突 | 阶段8已有冲突案例 | 执行前 `fetch/pull --ff-only`，禁止强推 |
| Connector 执行身份可能漂移 | 身份错误会导致 PR 创建失败 | 重新授权后检查 PR 评论作者和 author association |

---

## 8. 标准工作流建议

### 8.1 轻量代码任务

```text
ChatGPT 制定任务卡或执行口径
Hermes 创建任务卡 / 记录云端状态
Codex 本地实现
Codex 运行测试
Codex 写 change-summary
Codex commit / push
Codex 用 gh 或 GitHub 插件创建 Draft PR
人工 review
需要时 Claude 做只读 review
Hermes 汇总结果
```

### 8.2 文档收口任务

```text
Codex fetch / pull --ff-only
确认 work/lingxi-codex-bootstrap
新增或更新 docs/collab/chatgpt-lingxi/reports/
必要时同步到 D:\dev\Dev-Document
commit / push 工作分支
不触碰 main
```

### 8.3 冲突处理原则

```text
遇到 fetch first：先 pull/rebase，不强推。
遇到 rebase conflict：列出冲突文件，人工决策保留或合并策略。
遇到 must be a collaborator：检查 GitHub Connector 实际执行身份。
遇到 gh auth 失败：优先修复本地 gh，不把 PAT 写进聊天或文件。
```

---

## 9. 5.1 收口口径

可采用以下口径描述当前环境：

```text
开发环境5.1版（多Agent初阶）已经形成初期稳定基线。
当前系统具备 Hermes 云端主控、Codex 本地施工、GitHub 双通道 PR 自动化、Claude 网页轻量 review、COS 归档的基础闭环。
5.1 后续重点不再是证明“能否跑通”，而是进入中期方案：流程固化、权限治理、任务模板标准化、复杂任务 review 能力增强。
```

---

## 10. 关键路径索引

| 类别 | 路径 |
|------|------|
| 本地 repo | `D:\dev\carrey-test-repo-1` |
| 云端 Hermes repo | `/home/ubuntu/workspace/repos/carrey-test-repo-1` |
| 任务卡 | `docs/collab/chatgpt-lingxi/tasks/` |
| 报告 | `docs/collab/chatgpt-lingxi/reports/` |
| 操作记录 | `docs/collab/chatgpt-lingxi/ops/` |
| repo doctor | `scripts/env5_repo_doctor.py` |
| FastAPI 应用 | `app/main.py` |
| FastAPI 测试 | `tests/test_health.py` |
| 5.0 历史基线 | `开发环境5.0版（Hermes替代OpenClaw）.md` |
| 5.1 当前基线 | `开发环境5.1版（多Agent初阶）.md` |
