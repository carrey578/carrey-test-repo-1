# 开发环境 4.6 版

> 用途：本文件用于替换《开发环境 4.5.1版.md》，作为当前实际开发环境、协作边界、操作基线与后续开发参考文档。  
> 口径：以当前已经实际落地、验证通过、可复验的环境为准，不再以早期规划稿为准。  
> 版本日期：2026-04-18

---

## 1. 文档定位与结论

《开发环境 4.6 版》对应的是一套**已经完成轻量协作闭环、已进入稳定使用态**的开发环境。

与 4.5.1 相比，4.6 的核心变化不是大规模扩容，而是把以下几件事真正跑通并固化下来：

1. **本地 Surface + GitHub 仓库 + Codex CLI** 的轻量协作链路已实际跑通。  
2. **阶段 7.1 ~ 7.5** 已完成，包含只读验证、最小写入试验、验收链修复、文档收口。  
3. **阶段 8** 已完成，已经形成“轻量任务按白名单执行”的稳定规则。  
4. 当前环境不再处于“继续搭建中”，而是处于：**可继续做低风险开发任务的稳定状态**。

当前建议的环境总定位是：

- **本地 Windows 轻终端**：主控、编辑、人工验收、Git 操作、Codex 本地轻量执行
- **腾讯云轻量服务器 / OpenClaw（灵犀）**：保留为云上执行与后续扩展能力，不作为当前轻量任务的默认入口
- **GitHub 测试仓库**：低安全约束、高授权、适合 AI 协作试验
- **ChatGPT Project + GitHub**：用于方案制定、文档产出、分阶段推进

一句话总结：

**开发环境 4.6 版 =“本地 Surface 为主、云上灵犀保留、Codex 已可轻量实用、GitHub 分支协作稳定可复验”的实际落地版。**

---

## 2. 当前环境总体架构

## 2.1 总体结构

当前采用的是：**本地轻主控 + 云上重执行保留位**。

### 本地（主控端）

用途：

- 日常开发入口
- PowerShell / Git / Codex CLI 执行
- Cursor 编辑与查看 diff
- 本地人工验收
- 关键提交与推送控制

当前本地角色已经从“仅操作终端”升级为：

**默认开发入口 + 默认轻量 Codex 执行入口**

### 云上（腾讯云轻量服务器）

用途：

- 保留 OpenClaw / 灵犀执行能力
- 适合作为后续更重型或更长链路的执行位
- 保留 GitHub 写回、Agent 常驻、容器类任务的扩展空间

当前云上角色不是关闭，而是：

**保留、可用、但不是当前轻量任务默认执行位**

### GitHub

用途：

- 唯一事实版本库
- 分支协作与提交记录基线
- 文档、脚本、状态文件统一沉淀

当前主工作分支：

- `work/lingxi-codex-bootstrap`

---

## 3. 当前主要资源与实际配置

## 3.1 本地设备

当前本地主力设备：

- Surface Laptop 4
- Windows 11
- PowerShell 作为主要命令行入口

当前本地职责：

- 打开仓库目录
- 运行 `git`、`codex`、PowerShell 脚本
- 使用 Cursor 查看与编辑文件
- 对 Codex 结果做最终人工验收

## 3.2 本地仓库目录

当前实际工作目录：

```text
D:\dev\carrey-test-repo-1
```

这是当前默认的本地开发根目录。

## 3.3 GitHub 仓库

当前测试仓：

- GitHub 用户名：`carrey578`
- 仓库：`carrey-test-repo-1`

仓库定位：

- 测试仓
- 可接受 AI 高权限试验
- 适合做环境打通、工作流验证、文档固化、低风险脚本调整

## 3.4 当前 Git 分支状态

当前主要工作分支：

```text
work/lingxi-codex-bootstrap
```

该分支在阶段 7、阶段 8 中持续使用，已经承载：

- Codex 只读验证
- 最小写入试验
- 验收脚本修复
- 文档收口
- 轻量协作白名单固化

当前推荐口径：

- **轻量协作默认继续在 `work/lingxi-codex-bootstrap` 上完成**
- 暂不强制切回其他分支
- 后续若进入真正开发任务，可再按任务单独开分支

## 3.5 SSH / Git 远端连通性

此前 GitHub SSH 曾出现 22 端口连接失败问题，已修复。

当前修复口径：

- 本地 `~/.ssh/config` 已加入 GitHub 的 **SSH over 443** 配置
- 当前 `github.com` 连接走 `ssh.github.com:443`

修复效果已验证：

- `ssh -T git@github.com` 认证成功
- `git fetch origin` 成功
- `git push origin work/lingxi-codex-bootstrap` 成功

因此，当前 Git 链路状态为：

**本地 pull / fetch / push 已恢复稳定可用。**

---

## 4. 本地工具现状

## 4.1 Git

已正常可用，用于：

- 查看状态
- 拉取远端
- 比较 diff
- 提交与推送

当前常用命令：

```powershell
Set-Location "D:\dev\carrey-test-repo-1"
git fetch origin
git pull --ff-only origin work/lingxi-codex-bootstrap
git status
git diff --name-only
git log --oneline -5
git push origin work/lingxi-codex-bootstrap
```

## 4.2 Cursor

已安装并可用。

当前角色：

- 主编辑器
- diff 查看器
- markdown / PowerShell 文件人工检查入口

## 4.3 Node / npm

根据当前已验证输出，本地环境可用版本为：

- Node.js：`v24.14.0`
- npm：`11.9.0`

说明：

- 当前脚本链路可运行
- 不需要因为阶段 8 已完成而额外升级 Node / npm
- 后续如无明确报错，不建议主动折腾版本

## 4.4 Codex CLI

当前状态：**已登录、已可执行、已完成实际任务验证**。

已知完成过的关键动作：

1. `codex login` 成功  
2. `codex exec` 可在本地仓库目录中运行  
3. 已完成只读总结任务  
4. 已完成最小写入试验  
5. 已完成阶段 7.3 / 7.4 / 7.5 / 8 对应的轻量文档与低风险修改任务

说明：

- 早期曾遇到参数差异、输出文件编码、shell policy 等问题
- 这些问题已经绕开或固化为可执行经验
- 当前不需要再重复做“Codex 能不能用”的基础验证

当前建议定位：

**Codex CLI 已从“试点工具”变成“可用于低风险任务的正式轻量工具”。**

---

## 5. 云上环境现状

## 5.1 腾讯云轻量服务器

当前仍然保留，作为云上执行位与后续扩展位。

此前定位包括：

- OpenClaw / 灵犀常驻
- GitHub 写回
- 云上脚本执行
- 容器或更重任务尝试

当前建议定位：

- **保留，不下线**
- **当前不作为轻量任务默认入口**
- **后续需要长时运行、容器、Agent 常驻、批处理时再启用为主入口**

## 5.2 OpenClaw / 灵犀

当前状态：

- 仍保留其能力与位置
- 仍是你整体多 Agent / 云上执行方案中的重要角色
- 但在 4.6 的默认日常动作中，不再承担最小任务的首选执行位

原因：

- 本地 Surface + Codex CLI 已经足够承担轻量任务
- 直接走本地更短路径、更可控、更容易人工验收
- 云上入口保留给更复杂、更重的任务更合适

因此，4.6 的默认建议是：

- **轻任务：先本地 Codex**
- **重任务/长任务/Agent 常驻任务：再考虑灵犀**

---

## 6. 仓库内关键目录与用途

以下目录与文件，是当前 4.6 环境里真正高频、有用、应继续保留的结构。

## 6.1 协作文档目录

```text
docs/collab/chatgpt-lingxi/
```

这是当前最核心的协作沉淀目录，用于：

- 阶段性方案
- session log
- playbook
- closeout 文档
- 状态记录
- ChatGPT / 灵犀 / Codex 协作文件

当前这条目录已经不是“规划中的共享目录”，而是：

**已经实际跑起来的协作中枢目录。**

## 6.2 工具脚本目录

```text
tools/
```

当前关键脚本包括：

- `tools/dev-entry.ps1`
- `tools/verify-dev-entry.ps1`
- `tools/repo-health-check.ps1`
- `tools/preflight-check.ps1`

它们目前组成了一个**可实际使用的轻量开发入口 + 验收链**。

## 6.3 其他关键目录

```text
scripts/
docs/
```

当前检查脚本已验证这些关键目录结构完整。

---

## 7. 当前脚本链路的实际可用状态

## 7.1 dev-entry 统一入口

当前命令：

```powershell
.\tools\dev-entry.ps1 help
```

已验证可正常输出帮助信息，当前可见命令包括：

- `check`
- `status`
- `docs`
- `handoff`
- `reports`
- `all`
- `preflight`
- `report`
- `help`

其中，`handoff` 当前文案已经在阶段 7.3 中优化为更贴近实际用途的说法。

当前定位：

**本地最简统一开发入口。**

## 7.2 verify-dev-entry 验证脚本

当前命令：

```powershell
.\tools\verify-dev-entry.ps1
```

当前稳定结果：

- 5/5 通过

此前该脚本曾存在 help 输出捕获不稳的问题，已在阶段 7.3 中修复。

当前定位：

**面向 `dev-entry.ps1` 的快速功能回归脚本。**

## 7.3 repo-health-check 健康检查

当前命令：

```powershell
.\tools\repo-health-check.ps1
```

当前稳定结果：

- 3/3 通过

已验证它能够串联：

- 本地开发环境检查
- preflight 检查
- dev-entry 验证

此前该脚本对子脚本退出码读取不稳的问题，也已经在阶段 7.3 中修复。

当前定位：

**本地最简总体验收入口。**

## 7.4 preflight-check

当前在阶段 7.4 中只做过一处极小的用户可见文案优化，没有改动逻辑边界。

当前定位：

**提交前低成本自检脚本。**

---

## 8. 阶段推进实际完成情况

这里不再沿用早期“计划完成度”的口径，而只记录**已经实做且有结果的阶段**。

## 8.1 阶段 6

已完成。

完成内容：

- 试点评估
- 回退规则
- 下一步决策文档固化

对应意义：

- 不再停留在“能不能试”的讨论层
- 把阶段 7 的前置条件说清楚了

## 8.2 阶段 7.1

已完成。

完成内容：

- 本地 Windows Codex 登录成功
- 本地只读验证通过
- 确认 Codex 可读取仓库文件
- 未改任何仓库文件

对应意义：

- 证明“本地 Surface 路线”是可走通的
- 为后续最小写入试验提供前提

## 8.3 阶段 7.2

已完成。

完成内容：

- 补记阶段 7.1 到 session log
- 确认只读验证结果有正式记录

对应意义：

- 把“试过了”变成“有记录、可追溯”

## 8.4 阶段 7.3

已完成，但属于**加固版完成**，不是“原版一次过”。

真实过程：

1. 先尝试对 `tools/dev-entry.ps1` 做单文件低风险文案修补  
2. 发现验收链不可靠  
3. 按失败路径回退  
4. 修复：  
   - `tools/verify-dev-entry.ps1`  
   - `tools/repo-health-check.ps1`  
5. 再次执行目标改动并通过验收

对应意义：

- 不是只证明“能改一个文件”
- 而是把最关键的验收链一起修稳了

这是 4.6 版里非常重要的一次真正落地。

## 8.5 阶段 7.4

已完成。

完成内容：

- 选择 `tools/preflight-check.ps1` 做单文件、文案级最小修改
- 验收通过
- session log 已补齐

对应意义：

- 证明阶段 7.3 之后，轻量脚本文案调整已经可以稳定重复执行

## 8.6 阶段 7.5

已完成。

完成内容：

- 形成阶段 7 总结收口文档
- 远端 push 问题修复
- 最终远端同步完成

对应意义：

- 阶段 7 不再是“散落的验证动作”
- 已经成为完整闭环

## 8.7 阶段 8

已完成。

完成内容：

- 形成轻量任务白名单规则文档
- 更新 session log
- 本地验收通过
- 提交、推送完成

对应意义：

- 当前环境从“试运行”进入“稳定规则驱动的轻量使用态”

当前阶段 8 的统一口径应为：

**阶段 8 已完成，当前已进入“轻量任务按白名单执行”的稳定状态。**

---

## 9. 当前协作规则：什么可以做，什么不要做

## 9.1 当前推荐动作（白名单）

当前最适合继续让 Codex 在本地执行的任务：

1. **文档类修改**
   - `docs/` 下 markdown 文档新增、补充、收口
   - session log 更新
   - playbook / summary / closeout 文档补全

2. **脚本文案级调整**
   - help 文案
   - 注释
   - 输出提示语
   - 标题文本

3. **低风险结构整理**
   - 不改逻辑的格式整理
   - 不改参数的文字优化
   - 不改退出码的轻微可读性优化

4. **固定验收流程**
   - `git diff`
   - `dev-entry help`
   - `verify-dev-entry`
   - `repo-health-check`

## 9.2 当前不建议动作

当前不建议直接让 Codex 自由执行的事项：

1. 一次性多文件大改  
2. 脚本逻辑重构  
3. 参数名、函数名、路径规则调整  
4. 云上灵犀与本地 Codex 同时大范围混用改同一批文件  
5. 没有明确验收动作就直接提交  
6. 为了“继续推进阶段”而人为制造新工作量

当前原则不是“尽量多做”，而是：

**必要、够用、可验收。**

---

## 10. 当前标准操作流（后续开发建议默认遵循）

后续若继续做轻量任务，建议默认使用以下最简流程。

## 10.1 任务开始前

```powershell
Set-Location "D:\dev\carrey-test-repo-1"
git fetch origin
git pull --ff-only origin work/lingxi-codex-bootstrap
git status
```

预期：

- 分支正确
- 与远端同步
- 工作区干净

## 10.2 执行轻量修改

优先方式：

- 让 Codex 在本地执行受限小任务
- 或人工用 Cursor / PowerShell 修改

要求：

- 尽量控制在 1 个文件
- 尽量是文档或文案层
- 不无谓扩张到逻辑层

## 10.3 本地验收

```powershell
.\tools\dev-entry.ps1 help
.\tools\verify-dev-entry.ps1
.\tools\repo-health-check.ps1
```

预期：

- help 正常
- verify 通过
- repo health 通过

## 10.4 检查改动范围

```powershell
git diff --name-only
git diff
```

要求：

- 先确认文件数量
- 再确认改动是否超边界

## 10.5 提交与推送

```powershell
git add <files>
git commit -m "<message>"
git push origin work/lingxi-codex-bootstrap
```

## 10.6 必要时补文档

以下情况建议补 session log 或总结文档：

- 新开一个阶段动作
- 首次尝试一种新协作方式
- 出现失败并回退
- 修复验收链或基础设施
- 需要后续复用时

---

## 11. 当前环境的实际优点与限制

## 11.1 当前优点

1. **短链路**  
   轻量任务直接在本地 Surface 完成，比绕云上更快。

2. **可复验**  
   已经形成固定的本地验收链，不靠口头判断。

3. **低成本**  
   当前不需要引入额外 API 计费、额外服务器或复杂基础设施。

4. **边界清楚**  
   已经有阶段 8 的白名单规则，知道什么适合做、什么不适合做。

5. **可逐步扩展**  
   本地路线稳定后，后续仍可把灵犀、Hermes、更多 Agent 作为扩展层加入，而不是推翻重来。

## 11.2 当前限制

1. 当前更适合**轻量任务**，不适合马上上高复杂多文件改造。  
2. 云上灵犀与本地 Codex 的协同规则虽已有基础，但当前默认策略仍是“本地优先”。  
3. 当前仓库是测试仓，适合试验，但不等于生产级治理。  
4. 本地 Codex 虽已可用，但仍应保持小范围、强验收、强人工复核。  
5. 某些 shell / 编码 / 输出细节曾出现过问题，后续仍建议尽量沿用已验证命令模板。

---

## 12. 当前是否需要继续升级环境

当前结论：

**不需要立刻继续升级。**

原因：

1. 当前 4.6 环境已经满足轻量开发和协作验证需要。  
2. 阶段 7 与阶段 8 的目标已经完成。  
3. 再继续做“环境工程”边际收益不高。  
4. 更合理的下一步，是把环境用于真实小任务，而不是继续扩建环境本身。

因此，当前推荐状态是：

**收口，进入维护态 / 使用态。**

---

## 13. 后续建议（最小必要动作）

这里不再给大规模新规划，只给最小必要建议。

## 13.1 建议一：把 4.6 作为当前唯一参考基线

后续若再看环境说明，优先看本文件，不再回到 4.5.1 的旧口径。

## 13.2 建议二：轻量任务优先直接用本地 Codex

适用场景：

- 文档补充
- playbook 更新
- 低风险 help / 提示语修改
- 小范围脚本文案优化

## 13.3 建议三：需要重任务时再启用云上灵犀

适用场景：

- 长时间运行
- 容器 / 常驻 Agent
- 更复杂链路
- 多步骤自动执行

## 13.4 建议四：有真实需求再开新阶段

建议只在以下情况下再开“阶段 8.x / 9”：

- 需要一条新的真实协作链
- 需要引入灵犀与 Codex 的明确分工
- 需要从文档/文案层进入真正脚本逻辑层
- 需要引入 API 计费或更强自动化

否则，不建议为了形式继续开阶段。

---

## 14. 当前统一状态口径（可直接复用）

当前开发环境可以统一描述为：

> 当前《开发环境 4.6 版》已经完成从 4.5.1 规划态向实际落地态的切换。  
> 本地 Surface + GitHub + Codex CLI 的轻量协作链路已跑通并稳定；阶段 7.1 ~ 7.5 与阶段 8 已完成，当前已进入“轻量任务按白名单执行”的稳定状态。  
> 日常轻量任务默认走本地 Codex + 本地验收；云上灵犀保留为后续重任务与扩展能力，不再作为当前默认最短路径。

---

## 15. 附：当前最常用的最简命令清单

### 15.1 进入仓库并同步

```powershell
Set-Location "D:\dev\carrey-test-repo-1"
git fetch origin
git pull --ff-only origin work/lingxi-codex-bootstrap
git status
```

### 15.2 查看统一入口

```powershell
.\tools\dev-entry.ps1 help
```

### 15.3 跑验收

```powershell
.\tools\verify-dev-entry.ps1
.\tools\repo-health-check.ps1
```

### 15.4 查看改动

```powershell
git diff --name-only
git diff
```

### 15.5 提交与推送

```powershell
git add <files>
git commit -m "<message>"
git push origin work/lingxi-codex-bootstrap
```

---

## 16. 最终结论

《开发环境 4.6 版》对应的是一套：

- 已实际落地
- 已完成轻量协作闭环
- 已有固定验收入口
- 已能支持后续小任务继续开发
- 当前适合收口并进入稳定使用态

后续开发时，建议默认先问两个问题：

1. 这次是不是轻量任务？  
2. 能不能先按 4.6 的本地最短路径完成？

如果答案是“能”，那就优先：

**本地 Surface + Codex + Git diff + 本地验收 + GitHub 分支提交。**

这就是当前最实际、最省力、最稳的开发环境主路径。
